{
  "name": "totemora",
  "version": "0.12.0",
  "private": true,
  "type": "module",
  "packageManager": "bun@1.3.14",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "demo:tribe": "bun run totemora run \"分析这个 demo 项目的订单折扣实现，找出与 README 业务规则不一致的风险，并给出有文件证据的改进建议，不修改文件\" --workspace examples/demo-project --accept \"逐条比较 README 业务规则与当前实现\" --accept \"每个关键结论引用真实文件路径\" --accept \"给出按优先级排序的改进建议\" --data-dir .totemora",
    "benchmark:smoke": "bun run totemora benchmark run --suite benchmarks/read-only-smoke.json --strong-member deepseek_reasoner --cheap-member qwen_worker --chief deepseek_reasoner --config-dir configs/example --data-dir .totemora",
    "benchmark:e5": "bun run totemora benchmark run --suite benchmarks/cross-domain-proof-v1.json --strong-member deepseek_reasoner --cheap-member qwen_worker --chief deepseek_reasoner --max-context-bytes 32000 --max-output-tokens 1600 --max-members 2 --max-total-tokens 40000 --config-dir configs/example --data-dir .totemora",
    "stability:drill": "bun run scripts/run-stability-drills.ts --data-dir .totemora --config-dir configs/example",
    "docs:check": "bun run scripts/check-docs.mjs",
    "dev:web": "bun --watch --no-clear-screen run packages/server/src/index.ts",
    "start:web": "bun run packages/server/src/index.ts",
    "mcp:stdio": "bun run packages/mcp/src/stdio.ts",
    "test:e2e": "playwright test",
    "storage:migrate": "bun run packages/server/src/storage-cli.ts migrate",
    "storage:verify": "bun run packages/server/src/storage-cli.ts verify",
    "telegram:doctor": "bun run packages/server/src/telegram-cli.ts doctor",
    "telegram:discover": "bun run packages/server/src/telegram-cli.ts discover",
    "telegram:setup": "bun run packages/server/src/telegram-cli.ts setup",
    "telegram:test": "bun run packages/server/src/telegram-cli.ts test",
    "reminder": "bun run packages/server/src/reminder-cli.ts",
    "deals": "bun run packages/server/src/deals-cli.ts",
    "forwarded": "bun run packages/server/src/forwarded-cli.ts",
    "intelligence:evidence": "bun run packages/server/src/intelligence-evidence-cli.ts",
    "notification:setup-legacy-ntfy": "bun run packages/server/src/notification-targets-cli.ts",
    "lint": "bun run scripts/check-workspace.mjs lint",
    "totemora": "bun run packages/tui/src/cli.ts",
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "@types/bun": "^1.3.14",
    "@types/node": "^25.9.1",
    "typescript": "^6.0.3"
  }
}
