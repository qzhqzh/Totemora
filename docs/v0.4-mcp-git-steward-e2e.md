# v0.4 MCP Git 提交专员真实验收

> 历史验收记录：该固定 Qwen Reviewer + 本地 Commit 架构已被 v0.5 Git Flow 状态机取代。

- 日期：2026-07-14
- 目标仓库：`/home/zhuqin/star/app/personal_site`
- 目标分支：`chore/totemora-commit-example`
- 入口：Streamable HTTP MCP `/mcp`

## 验收目标

通过外部 MCP Client 发起持久任务，确认 Chief 把任务交给由 DeepSeek 火种孵化的 Git 提交专员；专员按用户 Git Flow v2 安全原则只提交当前目标文件，独立 Reviewer 验收后执行固定验证和本地 Commit，不 push，不混入较早遗留改动。

## 第一次运行与修复

第一次任务 `2e2eb3b8-251b-40ef-a344-48aba8f7ffa2` 在独立 Reviewer 阶段失败：Xiaomi Provider 返回 `401 Invalid API Key`。Chief 与 DeepSeek 专员阶段已经完成，但没有生成可批准 Proposal，也没有触碰目标仓库。

修复后由 Qwen 成员 `qwen_worker` 承担独立 Reviewer，保持与 DeepSeek 专员异构，并继续禁止 Chief 自审。

## 成功运行

- Task：`49d1db66-1dd6-4940-9e0b-a3f3ea71b52b`
- Proposal：`9b46574e-3b9d-40e1-badf-e5b3aef3b61d`
- Chief：`deepseek_reasoner`
- Specialist：`deepseek_git_steward`
- Reviewer：`qwen_worker`
- Skill：`git-change-management` v2
- Git Flow 状态：无 develop、无未推送 Commit、无 stash
- 批准文件：`test`
- 保留未提交：`.opencode/`、`opencode.json`
- Commit message：`test: add test commit`
- Validation：`test "$(cat test)" = "test commit"`，exit code 0
- Commit：`82c0dfb77d99f73ecb948729acecd5603b670799`
- Push：未执行；分支无 upstream

成功经验已写入 `.totemora/member-experience/deepseek_git_steward.json`，包含 Proposal、Reviewer、验证命令和 Commit SHA。
