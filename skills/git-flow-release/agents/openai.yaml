interface:
  display_name: "Git Flow & Release"
  short_description: "一次授权完成 Commit、PR、Merge 与主线收口"
  default_prompt: "Use $git-flow-release to take the current change continuously through the authorized Commit, PR, Merge, or release endpoint."
